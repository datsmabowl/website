# images

Static images only (jpg / png / webp). For most projects you'll use short looping
video clips instead — drop those in the `/video/` folder (see below).

To use an image as a grid thumbnail, set `media: "images/myfile.jpg"` in `script.js`.
